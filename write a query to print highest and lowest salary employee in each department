Problem Statement : write a query to print highest and lowest salary employee in each department 

Tables : 

create table employee 
(
emp_name varchar(10),
dep_id int,
salary int
);
delete from employee;
insert into employee values 
('Siva',1,30000),('Ravi',2,40000),('Prasad',1,50000),('Sai',2,20000);


Solution : 

 with cte as (
 select *,dense_rank() over(partition by dep_id order by salary asc ) min_salary,dense_rank() over(partition by dep_id order by salary desc ) max_salary  from employee ) 
 select 
 dep_id,
 max(case when min_salary=1 then emp_name END) as min_salary_emp , 
 max(case when max_salary=1 then emp_name END) as max_salary_emp 
 from cte group by dep_id;
